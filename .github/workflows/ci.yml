# .github/workflows/ci.yml
name: ci

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: brikaya-qa-${{ github.ref }}
  cancel-in-progress: false

jobs:
  ci:
    uses: ./.github/workflows/ci-reusable.yml
    secrets: inherit
