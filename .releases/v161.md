---
release: v161
title: Brikaya v161
prerelease: false
---

## Destaque

- Gate `verify:version-docs-results` obrigatório no ship e no deploy — rejeita placeholders, exige resultados reais na descrição da tag e nas notas da release
- Paridade tri-engine de artefatos de IA: Claude, Cursor e Codex convergiram em políticas, hooks, skills e comandos idênticos para o fechamento de prompt (PR+merge+tag+release automático)
- Detecção de projeto brikaya no `project-detection.py` global e skip de auto-merge genérico para respeitar o pipeline `brikaya:ship`

## Mudanças

- Novo script `scripts/validate-version-doc-results.mjs` (`npm run verify:version-docs-results`) que valida preenchimento de resultados — bloqueia descrição vazia, placeholders de scaffold, bullets vazios em seções de release
- Novo test `scripts/validate-version-doc-results.test.mjs` com casos de cobertura (tag preenchida/vazia/placeholder, release com/sem bullets, seções faltando)
- Wired em `brikaya:ship` (bloqueia PR) e em `.github/workflows/deploy-production.yml` (bloqueia publicar release)
- Projeto Claude: `CLAUDE.md` + `.claude/settings.json` + 3 hooks (git-route, visual-asset-policy, ship-reminder) + 4 comandos slash + 1 skill + 1 automação + parity rule
- Cursor: novo `.cursor/rules/brikaya-release-on-completion.mdc` (alwaysApply) + atualizado hook ship-reminder
- Codex: `AGENTS.md` seções "Fechamento de prompt" e "Validação mínima" com a pipeline completa
- Tri-engine rules em `.cursor/rules/ai-artifact-parity.mdc` e `~/.cursor/rules/ai-artifact-parity.mdc` + atualizado `CLAUDE.md` e `~/.claude/CLAUDE.md` com tabela de equivalência

## Validação

- Menu/Logs: Versão v161 / Version v161
- Produção: https://brikaya.com/
- `npm run test:version-docs:unit` ok (novo teste incluído)
- `npm run verify:version-docs-results` valida v161.md preenchido
- Enforcement blocado v160 com placeholders (comportamento esperado)
